[project]
name = "digest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.97.0",
    "click>=8.3.3",
    "dotenv>=0.9.9",
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
    "yfinance>=1.3.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
]
